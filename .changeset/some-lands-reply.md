---
'@faith-tools/sensible-ui': patch
---

Introduce CSS for description lists (`<dl>`) with variants for standard lists and a card-like display. This includes styling for `<dt>` and `<dd>` elements to ensure proper spacing, borders, and text wrapping. Also adds example usage to the home page.
