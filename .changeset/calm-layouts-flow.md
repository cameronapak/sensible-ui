---
"@faith-tools/sensible-ui": minor
---

Add an optional generated CSS utility stylesheet with token-backed spacing, sizing, flexbox, grid, and alignment helpers. Add stack, cluster, split, and intrinsic grid layouts to the core bundle.
