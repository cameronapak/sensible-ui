---
"@faith-tools/sensible-ui": minor
---

Make every component subpath export standalone by including its required theme and base styles. Move shared card styling into the card module so importing `@faith-tools/sensible-ui/card` provides the complete card interface.
