---
'@faith-tools/sensible-ui': patch
---

Make cross-document page view transitions opt-in through the `@faith-tools/sensible-ui/view-transition` export instead of enabling them in the default bundle.
