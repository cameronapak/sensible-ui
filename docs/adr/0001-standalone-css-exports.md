# Make CSS subpath exports standalone

Each component subpath export includes its required theme and base styles. This makes every published import path usable on its own, at the accepted cost that importing any component also applies Sensible UI's semantic defaults.
